version: 2.1
jobs:
  run_tests:
    docker:
      - image: circleci/node:10
    steps:
      - run:
          name: install dependecies
          command: |
            npm install --save
      - run:
          name: run test
          command: |
            npm run test
workflows:
  build_test:
    jobs:
      -run_tests